# jinhualunCode Project Rules

## 1. 规则目的

本文件用于约束 `jinhualunCode` 项目的需求理解、产品设计、技术实现、Agent 集成、交付方式与质量标准。

这不是通用前端规范，而是针对“资管渠道销售内部工具”这一笔试项目的专项规则。

目标是确保项目始终围绕题目核心场景推进，避免偏题、过度设计、功能堆砌或工程失控。

## 2. 规则适用范围

- 适用于 `D:\wqxCode\wqx_code\jinhualunCode` 目录下的全部设计、代码、文档和演示产物
- 适用于页面设计、数据建模、交互设计、Mock 数据、Agent 方案、测试与交付文档
- 适用于后续实现阶段的所有技术决策和范围取舍

## 3. 规则优先级

当多个文档或约束出现差异时，优先级如下：

1. 原始 PDF 题目
2. `PRD.md`
3. `RULES.md`
4. 具体实现说明、注释、提交说明

如果 `RULES.md` 与 `PRD.md` 冲突，以 `PRD.md` 为准。

如果 `PRD.md` 与 PDF 冲突，以 PDF 原始要求为准。

## 4. 项目总体原则

- 必须始终围绕“产品货架 + 客户管理 + 数据概览”三大核心模块展开
- 必须优先体现业务理解，而不是只做机械式 CRUD
- 必须优先保证核心链路可用，再考虑加分项
- 必须用最小复杂度完成最有说服力的功能闭环
- 必须让面试官能一眼看出设计取舍和工程判断
- 禁止为了炫技引入与题目价值不匹配的复杂架构
- 禁止为了“功能丰富”而牺牲完成度、稳定性和体验一致性

## 5. 范围控制 Rules

### 5.1 必做范围

以下内容必须实现：

- 产品货架模块
- 客户管理模块
- 数据概览 Dashboard 模块
- 异步数据获取机制
- 基础响应式布局
- 加载态、空态、异常态
- 最终可演示的在线 Demo
- 仓库中的 `DESIGN.md`

### 5.2 建议优先级

功能优先级必须遵循以下顺序：

1. 数据建模与信息架构
2. 三个核心模块的基础可用版本
3. 关键交互与边界状态
4. Dashboard 业务价值提升
5. Agent 加分项
6. 工程化增强项

### 5.3 明确不做

首版默认不做以下内容，除非核心范围已全部稳定完成：

- 登录鉴权
- RBAC 权限系统
- 多租户或多组织
- 真实基金交易流程
- 审批流
- 复杂导出系统
- 复杂报表中心
- 真实 CRM 或基金交易系统对接
- 与题目无关的营销型页面

## 6. 业务理解 Rules

- 所有功能都必须回答一个问题：它是否真实帮助“渠道销售人员”提升工作效率
- 页面中的每个字段、图表、筛选项都应有业务意义
- 不允许堆放“看起来专业”但没有业务用途的数据字段
- 不允许用无关的互联网产品逻辑替代资管销售场景
- 客户管理必须体现客户关系维护，而不是只保留一个名字和电话
- 产品货架必须体现资管产品属性，而不是泛商品列表
- Dashboard 必须服务销售判断，而不是只展示图表组件能力

## 7. 用户体验 Rules

- UI 必须服务于“高频查阅”和“快速定位信息”
- 页面层级必须清晰，避免将所有字段堆在同一个视图里
- 重要信息必须具备明显的视觉优先级
- 列表页必须支持快速筛选与快速定位
- 详情页必须突出业务关键信息，而不是做成纯资料卡
- 新增跟进记录的流程必须足够轻量，不能过度复杂
- 页面必须覆盖桌面端和基础移动端宽度
- 所有核心页面都必须有加载态、空态、异常态
- 错误提示必须让用户知道发生了什么，以及下一步能做什么

## 8. 信息架构 Rules

### 8.1 页面层级

首版信息架构必须至少包含：

- 产品列表页
- 产品详情查看区域
- 客户列表页
- 客户详情页
- Dashboard 页

### 8.2 页面职责

- 产品列表页负责快速筛选、搜索、预览产品
- 产品详情负责完整呈现基金产品关键信息
- 客户列表负责快速定位客户
- 客户详情负责集中展示客户信息、持仓和跟进记录
- Dashboard 负责宏观业务洞察，不承担复杂管理操作

### 8.3 视图切换

- 产品详情应优先选择高效率查看方式
- 如果用弹窗，必须保证信息完整性不受明显影响
- 如果用侧边栏，必须保证列表上下文仍可见
- 如果用独立页面，必须保证返回路径清晰

## 9. 数据建模 Rules

### 9.1 最低实体要求

项目数据层至少包含以下实体：

- `Product`
- `Client`
- `Holding`
- `FollowUpRecord`

### 9.2 产品数据 Rules

产品实体至少包含：

- `id`
- `name`
- `type`
- `nav`
- `aum`
- `status`

推荐扩展字段：

- `riskLevel`
- `manager`
- `inceptionDate`
- `currency`
- `tags`

产品字段设计必须满足以下要求：

- 可支撑搜索
- 可支撑筛选
- 可支撑详情展示
- 可支撑 Dashboard 图表聚合

### 9.3 客户数据 Rules

客户实体至少包含：

- `id`
- `name`
- `level`
- `contact`
- `company`
- `owner`

客户数据必须能体现客户经营价值，而不是仅有基础身份字段。

### 9.4 持仓关系 Rules

客户和产品之间必须建模为多对多关系。

禁止将“客户持有哪些产品”直接写死在 UI 层。

必须通过结构化的持仓关系数据承载关联逻辑。

持仓关系建议至少包含：

- `holdingId`
- `clientId`
- `productId`
- `amount`
- `positionStatus`

### 9.5 跟进记录 Rules

跟进记录必须至少包含：

- `id`
- `clientId`
- `date`
- `channel`
- `content`
- `relatedProductIds`

跟进记录必须体现“时间 + 渠道 + 内容 + 关联产品”四类核心信息。

禁止将跟进记录只做成一行无法追溯的备注。

### 9.6 数据规模 Rules

- 产品数据不能少于 8 个
- 客户数据不能少于 8 位
- 数据量必须足以演示筛选、搜索、持仓关系和 Dashboard
- Mock 数据不得过于单一，必须体现类型、状态、层级差异

## 10. 异步数据 Rules

- 所有业务数据必须通过异步方式获取
- 禁止在组件内直接 `import` 业务数据后直接渲染
- 必须存在独立的数据访问层或模拟 API 层
- 异步请求必须有延迟模拟或真实请求过程
- 异步失败场景必须可处理
- 后续如果切换到真实后端，UI 层不应需要大改

## 11. 产品货架模块 Rules

- 必须支持产品列表展示
- 必须支持产品搜索
- 必须支持按产品类型筛选
- 必须支持按产品状态筛选
- 筛选与搜索必须可组合使用
- 产品状态必须有清晰区分
- 产品详情必须展示完整关键字段
- 产品详情中应尽量体现业务可读性，例如规模、状态、风险级别等
- 若时间允许，可补充“该产品被哪些客户持有”的反查视图

禁止事项：

- 禁止把产品货架做成没有筛选能力的静态卡片墙
- 禁止只展示名称和净值，导致无法体现业务维度
- 禁止产品详情信息比列表还少

## 12. 客户管理模块 Rules

- 必须支持客户列表展示
- 必须支持查看客户基本信息
- 必须支持查看客户持有产品
- 必须支持查看客户跟进记录
- 必须支持新增客户跟进记录
- 客户详情必须把“客户信息 + 持仓 + 跟进”放在一个完整上下文中
- 跟进记录录入必须考虑高频使用场景

建议要求：

- 客户列表支持关键字段搜索
- 客户列表支持按等级或负责人筛选
- 持仓列表支持跳转到产品详情
- 跟进记录支持展示最近更新时间顺序

禁止事项：

- 禁止客户信息与持仓、跟进完全割裂
- 禁止跟进记录录入后没有可见反馈
- 禁止把客户关系管理做成只有静态详情的展示页

## 13. Dashboard Rules

- Dashboard 至少包含两个有意义的数据可视化
- 图表主题必须能解释为什么“对销售团队有价值”
- 图表必须使用项目内部真实业务数据聚合而来
- 图表标题必须明确、易懂，不允许只有抽象统计名词
- Dashboard 必须偏概览，而不是细节操作台

首版优先推荐图表：

- 按产品类型统计的规模占比
- 客户持仓类型分布、客户覆盖排行或跟进活跃度统计

禁止事项：

- 禁止只选“容易画”的图而不考虑业务价值
- 禁止图表与底层数据不一致
- 禁止出现只有视觉效果、没有解释意义的图表

## 14. Agent Rules

### 14.1 总体原则

- Agent 是重要加分项，但不能以牺牲核心功能为代价
- Agent 必须与业务数据、表单或可视化界面产生真实联动
- Agent 不能只做一个独立聊天框然后与系统主功能脱钩

### 14.2 推荐实现方向

首版优先选择以下方向中的一个或两个：

- 智能查询
- 辅助录入
- 自然语言驱动 Dashboard

### 14.3 智能查询 Rules

- 用户可以通过自然语言查询客户、产品、持仓或统计信息
- 返回结果必须结构化、可解释
- 查询结果应尽量落到现有业务界面或结果区中
- 查询答案必须来自当前项目数据，而不是模型自由生成

### 14.4 辅助录入 Rules

- 用户可通过自然语言描述跟进内容
- Agent 应解析出日期、沟通渠道、客户对象、意向产品、金额等关键信息
- 解析结果必须可回填到表单
- 用户必须能修改 Agent 解析结果后再提交

### 14.5 自然语言驱动 Dashboard Rules

- 若实现该能力，必须是“基于现有业务数据动态生成图表配置”
- 不允许只回复一段文本说明而没有图表结果
- 必须有兜底机制，避免用户输入无法识别时系统失控

### 14.6 Agent 架构 Rules

- Agent 层必须与 UI 层、数据层分离
- 必须区分“意图识别”“业务执行”“结果展示”三个阶段
- 若使用真实 LLM API，必须控制失败与超时场景
- 若使用 Mock，必须让 Mock 行为可解释且可演示

### 14.7 Agent 安全 Rules

- 禁止让 Agent 直接修改不可回退的重要数据
- 禁止让 Agent 直接执行未受控的任意命令
- 用户输入必须经过解析和约束，不能无边界透传

## 15. 技术架构 Rules

- 前端必须使用 React
- 允许自由选择工程栈，但必须说明为什么这样选
- 架构设计必须优先考虑可读性、可演示性和可扩展性
- 组件职责必须明确，避免巨型页面组件
- 数据状态与 UI 状态应适度分离
- 业务实体、Mock API、页面组件、图表逻辑、Agent 逻辑应分层组织

推荐组织方式：

- `features` 或按业务域组织代码
- `shared` 放通用组件与工具
- `mock` 或 `services` 承载异步数据访问逻辑
- `agent` 承载意图解析和业务执行封装

禁止事项：

- 禁止所有逻辑都堆在单个页面文件
- 禁止把领域数据结构散落在各组件中重复声明
- 禁止图表聚合逻辑直接硬编码在 JSX 内

## 16. 工程实现 Rules

- 新增依赖前必须能说明引入理由
- 依赖选择应优先少而稳，不追求“库越多越高级”
- 命名必须体现业务语义，而不是纯技术语义
- 类型定义必须优先服务业务模型清晰度
- 异常处理不能只 `console.log`
- 页面和组件必须保留足够的可读性，便于面试解释

如果使用 TypeScript，必须：

- 优先为领域模型建立清晰类型
- 避免 `any` 滥用
- 避免把复杂对象结构完全交给推断

如果使用状态管理库，必须：

- 仅在跨模块共享状态明显必要时引入
- 避免用全局状态承载纯页面临时状态

## 17. UI 设计 Rules

- 视觉设计应专业、简洁、稳定，符合企业内部工具气质
- 不应过度娱乐化、营销化或过度炫技
- 色彩、字号、间距必须有一致性
- 表格、卡片、筛选器、图表风格必须统一
- 状态色必须有明确语义

### 17.1 内部工具风格要求

- 优先强调清晰与效率
- 用设计表达信息权重，而不是靠堆砌装饰
- 页面必须让面试官感受到“可用于真实工作场景”

### 17.2 响应式要求

- 至少覆盖常见桌面宽度和基础移动端宽度
- 不能出现严重溢出、遮挡、不可点击问题
- 核心操作在窄屏下仍应完成

## 18. 文案与业务表达 Rules

- 文案必须专业、简洁、符合资管业务语境
- 不允许出现明显与金融场景不符的占位语
- 状态、字段名、按钮文案必须清楚表达业务动作
- 图表标题和统计卡片文案必须可被非技术评审直接理解

## 19. 测试与质量 Rules

- 每个核心模块至少做一次完整功能自测
- 必须验证筛选、搜索、详情查看、跟进新增、图表展示这些关键路径
- 必须验证异步数据加载和失败场景
- 如果实现 Agent，必须验证至少一个可复现的自然语言示例
- 如果时间允许，优先补关键逻辑测试，而不是追求测试数量

### 19.2 模块实现前置流程 Rules

- 每次实现模块或明显独立功能前，必须先完成方案梳理，不得直接编码
- 第一阶段输出必须包含：
- 相关模块清单
- 相关实体清单
- Mermaid 时序流转图
- Mermaid 数据流图
- 每个模块的输入与输出
- 推荐技术栈及其理由
- 第一阶段输出后必须等待用户确认
- 第二阶段输出必须包含：
- 实施分步方案
- 每步新增或修改的文件
- 受影响模块范围
- 关键风险、兼容性影响与验证方案
- 第二阶段输出后必须再次等待用户确认
- 未经第二次确认，禁止开始编写代码

### 19.1 最低验收清单

- 产品列表可展示、可筛选、可搜索
- 产品详情可查看
- 客户列表可展示
- 客户详情可查看持仓与跟进
- 跟进记录可新增
- Dashboard 至少两图
- 数据异步加载成立
- 核心状态反馈可见

### 19.3 编码后审查 Rules

- 每次完成代码实现后，必须执行一次额外的代码审查
- 该审查应由另一个 Agent 独立完成，而不是与实现步骤混在一起
- 审查重点必须覆盖正确性、潜在回归、边界条件、安全性与验证缺口
- 如果审查发现问题，必须先修复或明确记录，再结束当前任务

### 19.4 上下文压缩与续接 Rules

- 项目必须维护独立于聊天记录之外的压缩上下文文件
- 压缩上下文文件目录固定为 `docs/context/`
- 至少包含：
- `CURRENT_STATE.md`
- `DECISIONS.md`
- `WORKLOG.md`
- `HANDOFF.md`
- `TASK_BOARD.md`
- `REVIEW_LOG.md`
- 每次开始新任务或在新会话中继续开发前，必须先读取 `CURRENT_STATE.md`、`HANDOFF.md`、`DECISIONS.md`、`TASK_BOARD.md`
- 每次完成非平凡任务后，必须更新 `CURRENT_STATE.md`、`WORKLOG.md`、`TASK_BOARD.md`、`HANDOFF.md`
- 如果发生关键技术或产品取舍，必须更新 `DECISIONS.md`
- 如果完成了代码实现并执行了独立审查，必须更新 `REVIEW_LOG.md`
- 这些文件应作为项目续接、开发交接和后续 `DESIGN.md` 整理的重要依据之一
- 文件职责与优先级必须明确：
- `CURRENT_STATE.md` 负责当前状态与下一步，是当前进度的一手来源
- `TASK_BOARD.md` 负责任务状态，是任务流转的一手来源
- `DECISIONS.md` 负责关键决策，是取舍依据的一手来源
- `HANDOFF.md` 负责新会话恢复入口，应与 `CURRENT_STATE.md` 和 `TASK_BOARD.md` 保持一致
- `WORKLOG.md` 是历史记录，不得覆盖 `CURRENT_STATE.md` 与 `TASK_BOARD.md` 的当前状态
- 如果这些文件之间出现冲突，结束任务前必须先完成对齐

## 20. Demo Rules

- Demo 必须可运行
- Demo 必须有清晰演示路径
- Demo 中的数据必须足以展示核心价值
- 如果用真实 API，必须保证演示时不会因配置问题直接失效
- 如果用 Mock Agent，必须让 Mock 能稳定触发且演示清晰

建议提供一条明确演示路径：

1. 看 Dashboard 了解全局
2. 进入产品货架定位目标产品
3. 进入客户详情查看持仓和跟进
4. 演示一次跟进新增
5. 演示一次 Agent 查询或辅助录入

## 21. 文档交付 Rules

- 最终必须有 `DESIGN.md`
- `DESIGN.md` 必须覆盖题目要求的四部分内容
- `DESIGN.md` 的内容必须能解释为什么这样做，而不是只罗列做了什么
- `PRD.md`、`RULES.md`、`DESIGN.md` 三者之间必须保持一致

### 21.1 DESIGN.md 必须覆盖

- 需求分析
- 技术选型
- AI 协作日志
- 自我复盘

### 21.2 AI 协作日志 Rules

- 至少保留 2 个完整的 prompt -> AI 输出 -> 你的处理结果
- 必须体现“采纳 / 修改 / 放弃”的判断过程
- 不能把 AI 输出原样堆上去而没有你的工程判断

## 22. 取舍决策 Rules

遇到时间不足或方案冲突时，按以下顺序做取舍：

1. 保证题目硬性要求
2. 保证三大核心模块成型
3. 保证数据关系清晰
4. 保证体验完整度
5. 再做 Agent 和工程加分项

如果必须放弃某项能力，优先放弃：

- 炫技型视觉
- 非必要复杂动画
- 复杂后端
- 非核心图表扩展
- 非关键二级功能

禁止放弃：

- 异步数据获取
- 产品、客户、Dashboard 三大模块
- 基础边界状态
- `DESIGN.md`

## 23. 禁止事项总表

- 禁止偏离资管销售场景做成通用后台模板
- 禁止将题目理解成简单的列表展示练习
- 禁止同步直灌静态数据到组件
- 禁止只做 UI 壳子而没有数据关系
- 禁止只接入聊天框就声称实现了 Agent 能力
- 禁止将复杂度浪费在非核心模块
- 禁止引入大量依赖却无法解释原因
- 禁止缺少加载态、空态、异常态
- 禁止图表没有业务解释
- 禁止最终交付缺少 `DESIGN.md`

## 24. Definition of Done

只有同时满足以下条件，项目才算完成：

- 三个核心模块全部可演示
- 数据模型完整支撑业务关系
- 数据通过异步方式获取
- 页面具备基础响应式能力
- 关键状态处理完整
- Demo 可运行
- `DESIGN.md` 已完成
- 若实现 Agent，已形成可复现的业务演示路径
- 代码和文档可以自圆其说，能够支撑面试追问
